[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    integration: WorkflowExecutor 集成测试（mock LLM，较慢）
filterwarnings =
    ignore::DeprecationWarning
