# DevFlow CI — 永久协作规则

> 本文件是 AI 编码助手在 DevFlow CI 项目中的行为准则。  
> 每一个新 AI 实例必须在会话启动时完整读取并严格遵守。  
> 人类拥有最终解释权，但 AI 必须主动执行本文件中的所有仪式。

---

## 规则 1：会话开始仪式

**触发**：每次新对话开始时，AI 必须自动执行（无需人类指令）。

**步骤**：
1. 读取项目记忆文件（权威路径见 `docs/MEMORY_INDEX.md`）：
   - `AI_COLLABORATION_GUIDE.md`（根目录，协作规则）
   - `CURRENT_STATUS.md`（根目录，项目现状）
   - `docs/decisions.md`（架构决策，权威源）
   - `docs/LEARNINGS.md`（经验教训，权威源）
   - `BUG_TRACKER.md`（根目录，缺陷追踪）
   - `docs/AGENT_PROMPT_TEMPLATES.md`（Prompt 模板，权威源）
   - `docs/success_cases.json`（成功案例，权威源）
   - `docs/ARCHITECTURE.md`（系统架构）
   - `docs/CODING_STANDARDS.md`（编码规范）
   - `sessions/<最新>/summary.md`（上次会话总结）
2. 根目录的 `decisions.md`、`LEARNINGS.md` 等仅为指针文件，**必须读取 docs/ 下的完整内容**。
3. 如果某文件不存在，标注"缺失"并继续。
3. 向人类输出结构化简报：

```
┌─────────────────────────────────────────┐
│  🤖 DevFlow CI 助手 — 会话初始化完成      │
├─────────────────────────────────────────┤
│  项目阶段：<从 CURRENT_STATUS 提取>       │
│  遗留问题：<N 个>                         │
│  已知阻塞：<描述或"无">                   │
│  上次会话：<session-id>                   │
├─────────────────────────────────────────┤
│  建议任务（按优先级）：                    │
│  1. <从 CURRENT_STATUS 下一步计划提取>     │
│  2. ...                                  │
├─────────────────────────────────────────┤
│  请确认本次会话目标，或直接分派任务。        │
└─────────────────────────────────────────┘
```

---

## 规则 2：会话进行中仪式

**触发**：开发过程中，只要满足以下任一条件，立即执行：

| 事件 | 动作 | 目标文件 |
|------|------|----------|
| 产生非平凡设计决策（有 A/B 方案需选择） | 追加决策记录 | `docs/decisions.md` |
| 发现 Bug 或代码缺陷 | 追加到缺陷追踪 | `BUG_TRACKER.md` |
| Bug 修复完成、审查打回、踩坑教训 | 提取根因，写入经验库 | `docs/LEARNINGS.md` |
| 成功交付一个可复用功能模块 | 提取模式，写入案例库 | `docs/success_cases.json` |
| 遇到模糊需求、多选方案、边界不清 | 主动请求澄清，不得自行假设 | 对话中 |

**决策记录格式**（docs/decisions.md）：
```markdown
## 决策 #NNN：标题
- **日期**：YYYY-MM-DD
- **状态**：[提议 | 已采纳 | 已废弃]
- **上下文**：为什么需要做这个决策？
- **方案对比**：| 方案 | 优点 | 缺点 |
- **选择**：选定哪个方案？为什么？
- **影响**：影响哪些模块？
- **风险/代价**：放弃了什么？
```

**经验记录格式**（docs/LEARNINGS.md）：
```markdown
## 教训 #NNN：一句话标题
- **日期**：YYYY-MM-DD
- **来源**：Bug / 审查反馈 / 事故
- **类别**：[代码错误 | 设计缺陷 | 流程 | 安全 | 性能]
- **现象**：
- **根因**：
- **修复**：
- **预防措施**：
- **Prompt 改进**：是/否
```

---

## 规则 3：会话结束仪式（必须执行）

**触发**：人类表示会话结束，或 AI 判断当前任务批次已完成。

> ⚠️ **如果人类忘记提醒，AI 必须主动要求执行本仪式。**  
> 不可在记忆文件未更新的情况下结束会话。

**步骤**：

### 3.1 更新 CURRENT_STATUS.md
- 已完成的任务（标记为 [x] 或 ✅）
- 变更文件清单
- 新产生的问题
- 下一步计划

### 3.2 输出会话摘要（对话中）

```markdown
---

## 📋 会话总结 — <session-id>

### 基本信息
- **会话 ID**：YYYYMMDD-NN
- **日期**：YYYY-MM-DD
- **主要目标**：

### 完成情况
| 任务 | 状态 | 备注 |
|------|------|------|

### 变更文件清单
| 文件路径 | 变更类型 | 说明 |
|----------|----------|------|

### 新增决策
（如有）

### 新产生的问题 / 待办
1. [ ] ...

### 下步建议
1. ...
```

---

## 规则 4：Chat 切换流程

当需要开启新 Chat 窗口继续工作时：

1. **先完成规则 3**（会话结束仪式），确保所有记忆文件已更新。
2. AI 应主动提醒人类：**"当前会话状态已保存，可以关闭此窗口。新窗口启动后 AI 将自动读取记忆文件接续。"**
3. 新 Chat 窗口启动后，AI 执行规则 1（会话开始仪式）即可无缝接续。

---

## 规则 5：沟通与环境规范

### 5.1 代码输出
- 引用文件时必须包含完整相对路径和行号：`agents/planner.py#L45-L78`
- 代码块必须标注语言

### 5.2 进度报告
- 每完成一个非平凡任务（≥1 文件变更或 ≥50 行代码），主动输出简短进度
- 格式：`✅ 已完成：xxx | ⏳ 进行中：xxx | 📋 待处理：N 项`

### 5.3 模糊需求
- 当存在多选方案、边界不清、安全敏感问题时，主动请求澄清
- 格式：
  ```
  ⚠️ 需要澄清：[问题描述]
    选项 A：[描述] — 理由：
    选项 B：[描述] — 理由：
    推荐：选项 X
  ```

### 5.4 Git Bash 环境规范
- 路径使用正斜杠：`/c/Users/ava_t/Desktop/DevFlowCI`
- 激活虚拟环境：`source venv/Scripts/activate`
- 环境变量：`export KEY=value`（临时）或写入 `.env` 文件（永久）
- 目录切换：使用 `cd /c/path/to/dir` 格式

### 5.5 视觉前缀
| 前缀 | 用途 |
|------|------|
| 💡 | 建议 |
| ⚠️ | 警告 |
| 🚫 | 错误 |
| ℹ️ | 补充信息 |
| ✅ | 完成 |
| ⏳ | 进行中 |

### 5.6 终端命令执行规范

> ⚠️ **此规范在所有会话中永久生效，不可遗忘。**

| 规则 | 说明 |
|------|------|
| **逐条执行** | 每次只发送一条命令，等待输出结果后再发下一条。严禁使用 `&&`、`&`、`;` 将多条命令合并为一行。 |
| **等待完成** | 每条命令必须完全执行完毕并输出结果。若输出缺少明确完成标志，默认等待 5 秒后再继续。 |
| **超时处理** | 如果一条命令等待超过 **2 分钟**仍无输出，主动询问用户是否终止并分析原因。 |
| **Git Bash 兼容** | 所有命令使用 Windows Git Bash 兼容格式。正斜杠路径、`source venv/Scripts/activate` 激活虚拟环境、`export` 设置环境变量。 |
| **后台进程** | 如需启动长期服务（如 uvicorn），使用 `( nohup ... > log 2>&1 & )` 子shell包装方式，避免阻塞终端。 |

---

> **本文件是 DevFlow CI 项目的"AI 宪法"。**  
> 人类可以随时修订本文件。AI 发现规则有改进空间时应主动提出 PR。  
> 最后更新：2026-06-01（决策 #018：docs/ 为记忆文件权威源）
