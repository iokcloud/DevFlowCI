# AI 极致协作工作说明书

> **项目**：DevFlow CI 助手  
> **版本**：v1.0.0  
> **最后更新**：2026-05-31  
> **维护者**：人类 & AI 共同维护  
> **文档角色**：本文件是每一次 AI 协作会话的起点。每一个新 AI 实例必须在会话启动时完整阅读本文件。

---

## 目录

1. [协作核心原则](#1-协作核心原则)
2. [长期记忆体系](#2-长期记忆体系)
3. [会话协作仪式](#3-会话协作仪式)
4. [质量保障机制](#4-质量保障机制)
5. [经验沉淀与进化](#5-经验沉淀与进化)
6. [沟通与输出规范](#6-沟通与输出规范)
7. [工具与自动化](#7-工具与自动化)
8. [附录](#8-附录)

---

## 1. 协作核心原则

### 原则一：状态可恢复（State Recoverability）

> **每一次会话结束，都必须产生一份可供下一个 AI 实例无缝接续的会话总结。**

- 不依赖对话窗口中的任何内容作为长期记忆。对话窗口 = 临时工作区。
- 任何对项目有持久价值的信息，必须在会话结束前写入文件系统。
- 下一位 AI 助手必须仅凭读取项目文件，就能理解项目当前状态、待办事项和上次会话的产出。

**强制产物**（每次会话结束）：
- `sessions/<session-id>/summary.md` — 本次会话的结构化总结

### 原则二：决策透明化（Decision Transparency）

> **所有非平凡的设计决策必须记录原因与权衡（Trade-offs）。**

- "非平凡"的定义：任何在实现时有 A/B 两个以上方案且需要主观判断的选择。
- 记录格式：**决策内容 → 备选方案 → 选择理由 → 放弃方案的风险 → 决策日期与上下文**。
- 存储位置：`docs/decisions.md`（轻量级 ADR 格式）。

**反例**（不可接受）：
```
// 用 Redis 做缓存
```

**正例**（要求标准）：
```markdown
### 决策 #001：缓存层选用 Redis 而非内存 Map

- **日期**：2026-05-31
- **上下文**：需要为 CI 任务状态提供跨进程缓存
- **备选方案**：
  | 方案 | 优点 | 缺点 |
  |------|------|------|
  | 内存 Map | 零延迟、零运维 | 无法跨进程，重启丢失 |
  | Redis | 跨进程、持久化 | 运维成本+1，网络延迟 |
  | SQLite | 零运维、持久化 | 并发写瓶颈 |
- **选择**：Redis — 因为 DevFlow CI 本身就是多进程架构，跨进程共享是硬需求。
- **风险/代价**：开发者本地需要安装 Redis。已计划后续加 fallback 到内存模式用于单机开发。
```

### 原则三：记忆外化（Memory Externalization）

> **任何长期有用的上下文必须写入项目文件，不能仅依赖对话窗口。**

- 对话窗口的上下文在会话结束后永久消失。任何你在对话中学到的、对后续工作有用的信息，请写入对应的记忆文件（见第 2 章）。
- 如果你发现了一个新的有价值信息类别但现有记忆文件无法归类，**主动提出创建新文件**。

**触发问题**（AI 自问）：
> "如果下一个 AI 实例不知道我刚学到的这件事，它会犯错吗？"
> 如果答案是"会"，立即写盘。

### 原则四：失败即学习（Failure as Learning）

> **每个 Bug、审查失败、设计缺陷都要沉淀为可被后续 AI 实例消费的经验。**

- Bug 修复完成后，提取根因和模式，写入 `LEARNINGS.md`。
- 人类审查打回代码后，更新 `AGENT_PROMPT_TEMPLATES.md` 中对应的自检清单。
- 同一个错误不应发生两次——用规则把它挡在门外。

**处理流程**：
```
Bug 发生 → 修复 → 提取根因 → 写入 LEARNINGS.md 
                                → 判断是否需更新 Prompt 模板 
                                → 如是，更新 AGENT_PROMPT_TEMPLATES.md
```

---

## 2. 长期记忆体系

### 2.1 记忆文件清单与规范

| # | 文件路径 | 用途 | 维护者 | 更新触发条件 |
|---|----------|------|--------|-------------|
| 1 | `CURRENT_STATUS.md` | 项目当前状态的快照摘要 | AI（主要）/ 人类 | 每次会话结束；重要里程碑达成时 |
| 2 | `docs/decisions.md` | 架构与技术决策记录（轻量 ADR） | AI（撰写）/ 人类（确认） | 任何非平凡设计决策产生时 |
| 3 | `docs/LEARNINGS.md` | 从 Bug、失败、踩坑中提取的经验教训 | AI（提取）/ 人类（审核） | 每次 Bug 修复后；每次审查打回后 |
| 4 | `docs/success_cases.json` | 成功交付的模式库，含代码片段指纹 | AI（自动）/ 人类（确认） | 每次成功完成一个可复用功能模块后 |
| 5 | `docs/AGENT_PROMPT_TEMPLATES.md` | 面向不同任务类型的 AI 提示词模板 | AI（建议）/ 人类（审核） | 发现某类任务的 Prompt 模式奏效后；审查打回暴露出 Prompt 缺陷后 |
| 6 | `docs/ARCHITECTURE.md` | 系统架构图、模块关系、技术栈说明 | AI（起草）/ 人类（审核） | 架构变更时；新模块引入时 |
| 7 | `docs/CODING_STANDARDS.md` | 项目级编码规范 | 人类（定义）/ AI（遵守） | 人类发现风格不符合预期时 |
| 8 | `docs/MEMORY_INDEX.md` | 记忆文件索引与读取顺序 | AI | 记忆体系变更时 |
| 9 | `BUG_TRACKER.md` | 已知缺陷追踪 | AI / 人类 | 发现/修复 Bug 时 |
| 10 | `COLLABORATION_PROMPT.md` | AI 行为准则（精简版） | 人类 | 协作规则变更时 |
| 11 | `sessions/` | 按 session-id 归档的会话总结 | AI | 每次会话结束 |

### 2.2 各文件详细规范

#### 2.2.1 CURRENT_STATUS.md

**格式**：
```markdown
# 项目当前状态

> 最后更新：YYYY-MM-DD HH:MM（会话: <session-id>）

## 基本信息
- 当前阶段：[初始化 | 核心开发 | 功能完善 | 测试覆盖 | 发布准备]
- 当前 Sprint/目标：
- 最后完成的功能：
- 正在进行的任务：
- 被阻塞的任务及原因：

## 模块完成度
| 模块名称 | 完成度 | 测试覆盖 | 备注 |
|----------|--------|----------|------|
| 示例模块 | 80% | 有/无 | 备注说明 |

## 已部署环境
| 环境 | 版本 | 最后部署时间 | 状态 |
|------|------|-------------|------|

## 已知问题
| # | 问题描述 | 严重性 | 发现日期 | 状态 |
|---|----------|--------|----------|------|

## 下一步计划（按优先级）
1. [ ] 任务描述
2. [ ] 任务描述
```

**更新频率**：每次会话结束时必更新。

#### 2.2.2 docs/decisions.md

**格式**：轻量级 ADR（Architecture Decision Record），每条决策一个章节。

```markdown
## 决策 #NNN：决策标题

- **日期**：YYYY-MM-DD
- **状态**：[提议 | 已采纳 | 已废弃]
- **上下文**：为什么需要做这个决策？
- **方案对比**：
  | 方案 | 优点 | 缺点 |
  |------|------|------|
- **选择**：选定哪个方案？为什么？
- **影响**：这个决策会影响哪些模块？
- **风险/代价**：选择了这个方案意味着放弃什么？
- **废弃原因**（如状态为已废弃）：
```

#### 2.2.3 docs/LEARNINGS.md

**格式**：
```markdown
## 教训 #NNN：一句话标题

- **日期**：YYYY-MM-DD
- **来源**：Bug 链接 / 审查反馈 / 线上事故
- **类别**：[代码错误 | 设计缺陷 | 流程问题 | 安全 | 性能]
- **现象**：发生了什么？
- **根因**：为什么发生？
- **修复**：如何修复的？
- **预防措施**：以后如何避免？
- **Prompt 改进**：是否需要更新 AGENT_PROMPT_TEMPLATES.md？（是/否，如是，记录关联条目）
```

#### 2.2.4 docs/success_cases.json

**格式**（与 `memory/case_store.py` 实际存储一致）：
```json
{
  "version": "1.0.0",
  "last_updated": "2026-06-01T00:00:00+00:00",
  "cases": [
    {
      "case_id": "case-0001",
      "requirement": "用户需求原文",
      "plan": [{"module_name": "...", "description": "...", "dependencies": [], "type": "backend"}],
      "modules": [{"module_name": "...", "code": "...", "test_code": "...", "status": "passed"}],
      "created_at": "2026-06-01T00:00:00+00:00",
      "keywords": ["关键词1", "关键词2"]
    }
  ]
}
```

**说明**：权威路径为 `docs/success_cases.json`（`config.SUCCESS_CASES_FILE`）。根目录 `success_cases.json` 为指针文件。

#### 2.2.5 docs/AGENT_PROMPT_TEMPLATES.md

**格式**：
```markdown
## 模板目录

### 1. 新功能开发
**适用场景**：从零实现一个新的功能模块
**推荐 Prompt 模式**：
> [具体 Prompt 模板，含角色设定、约束、自检清单]

### 2. Bug 修复
**适用场景**：修复已报告的 Bug
**推荐 Prompt 模式**：
> [具体 Prompt 模板]

### 3. 代码重构
**适用场景**：不改变外部行为的前提下优化内部结构
**推荐 Prompt 模式**：
> [具体 Prompt 模板]

### 4. 代码审查准备
**适用场景**：提交 PR 前过自检
**推荐 Prompt 模式**：
> [具体 Prompt 模板]
```

### 2.3 会话启动时的记忆注入流程

每个新 AI 实例在会话开始时，**必须**按以下顺序读取并内化记忆：

```
步骤 1：读取本文件（AI_COLLABORATION_GUIDE.md）      —— 了解协作规则
步骤 2：读取 COLLABORATION_PROMPT.md（精简准则）     —— 了解 AI 宪法
步骤 3：读取 CURRENT_STATUS.md                      —— 了解项目现状
步骤 4：读取 docs/MEMORY_INDEX.md                   —— 确认记忆文件路径
步骤 5：读取 docs/ARCHITECTURE.md                   —— 了解技术架构
步骤 6：读取 docs/CODING_STANDARDS.md               —— 了解编码规范
步骤 7：读取 docs/LEARNINGS.md（最近 20 条）         —— 了解历史教训
步骤 8：读取 docs/AGENT_PROMPT_TEMPLATES.md          —— 了解当前任务匹配的模板
步骤 9：读取 docs/decisions.md（最近 30 天）         —— 了解近期决策
步骤 10：读取 docs/success_cases.json               —— 了解可复用模式
步骤 11：读取 sessions/<最新>/summary.md             —— 了解上次会话产出
步骤 12：确认当前会话任务目标                          —— 与人类对齐
```

**前置条件**：如果步骤 2-8 中某些文件尚不存在，AI 应明确告知人类"该记忆文件尚未初始化"，并在本次会话中留意是否有机会创建。

---

## 3. 会话协作仪式

### 3.1 会话开始仪式

#### AI 的自动初始化流程（无需人类指令）

1. **读取记忆**：按照 §2.3 的流程读取所有记忆文件。
2. **汇报状态**：输出一段结构化状态简报：

```
┌─────────────────────────────────────────┐
│  🤖 DevFlow CI 助手 - 会话初始化完成      │
├─────────────────────────────────────────┤
│  项目阶段：核心开发                        │
│  当前 Sprint：用户认证模块 v0.2            │
│  上次会话：session-20260530-01            │
│  未完成任务：2 个                          │
│  已知阻塞：无                             │
│  最近教训：3 条未解决                       │
├─────────────────────────────────────────┤
│  本次推荐任务：                            │
│  1. 完成上次会话遗留的 XXX 功能             │
│  2. 修复 LEARNINGS.md 中标记的已知缺陷      │
├─────────────────────────────────────────┤
│  请确认本次会话目标，或直接分派任务。        │
└─────────────────────────────────────────┘
```

3. **确认任务**：等待人类给出本次会话目标。如果人类给出的任务与 CURRENT_STATUS.md 中记录的"下一步计划"有冲突或依赖问题，AI 必须主动指出。

### 3.2 开发中仪式

#### 3.2.1 代码自检清单（每次生成代码块前）

```
□ 类型注解是否完整？（Python: mypy strict 兼容）
□ 是否包含 Docstring？（公开函数/类必须有）
□ 格式化是否合规？（项目配置的 formatter）
□ 异常处理是否充分？（外部调用、IO 操作必须有）
□ 是否有安全隐患？（用户输入校验、SQL 注入、路径遍历等）
□ 是否遵循项目 CODING_STANDARDS.md？
□ 是否有遗留的调试代码（print、console.log、TODO 无跟踪）？
□ 新增依赖是否已记录在 requirements 文件中？
```

#### 3.2.2 进度报告

- **默认频率**：每完成一个非平凡任务（≥1 个文件变更或 ≥50 行新代码），AI 主动输出简短进度。
  ```
  ✅ 已完成：实现 login_handler.py（3 文件变更，120 行新增）
  ⏳ 进行中：实现 token 刷新逻辑
  📋 待处理：2 项
  ```
- 人类可以随时要求更详细或更简洁的报告。

#### 3.2.3 遇到模糊需求时的澄清机制

当 AI 遇到以下任何一种情况时，**必须主动请求澄清**，不得自行假设：

| 触发条件 | 示例 |
|----------|------|
| 多选一方案无明确优劣 | "日志用 structlog 还是标准 logging？" |
| 输入/输出格式未定义 | "这个 API 返回 200 还是 201？" |
| 边界条件不明确 | "用户名最大多长？允许特殊字符吗？" |
| 性能/成本敏感 | "这个缓存 TTL 设为多少合适？" |
| 安全敏感 | "这个接口是否需要认证？" |
| 与现有决策冲突 | "你要求的方案与 decision #005 冲突，是否要推翻之前的决策？" |

**澄清格式**：
```
⚠️ 需要澄清：[一句话描述歧义]
  选项 A：[描述] — 理由：[简短理由]
  选项 B：[描述] — 理由：[简短理由]
  推荐：选项 X，因为 [理由]
  影响：[选择不同会影响什么]
```

### 3.3 会话结束仪式

> **重要**：会话结束仪式是强制执行的。即使人类说"可以了，结束吧"，AI 也必须完成以下所有输出和文件更新后才能"结束"。

#### 3.3.1 必须输出的内容（对话中）

```markdown
---

## 📋 会话总结 — <session-id>

### 基本信息
- **会话 ID**：<session-id>（格式: YYYYMMDD-NN）
- **日期**：YYYY-MM-DD
- **主要目标**：[一句话描述本次会话目标]

### 完成情况
| 任务 | 状态 | 备注 |
|------|------|------|
| 任务 1 | ✅ 完成 | |
| 任务 2 | ⏳ 部分完成 | 剩余 X 部分，因为 Y 原因阻塞 |
| 任务 3 | ❌ 未开始 | |

### 变更文件清单
| 文件路径 | 变更类型 | 行数变化 |
|----------|----------|----------|
| src/xxx.py | 新增 | +120 |
| src/yyy.py | 修改 | +15/-8 |
| docs/decisions.md | 新增 | +45 |

### 新增决策
- 决策 #NNN：标题 → [链接](docs/decisions.md#决策-nnn标题)

### 新增教训
- 教训 #NNN：标题 → [链接](docs/LEARNINGS.md#教训-nnn标题)

### 新产生的问题 / 待办
1. [ ] 问题描述 — 优先级：高/中/低
2. [ ] 问题描述 — 优先级：高/中/低

### 下步建议
1. （最优先）建议下一个 AI 实例完成 XXX
2. 考虑重构 YYY 模块以提升 ZZZ
```

#### 3.3.2 必须更新的文件

| 优先级 | 文件 | 操作 |
|--------|------|------|
| P0（必做） | `CURRENT_STATUS.md` | 更新所有章节，反映最新状态 |
| P0（必做） | `sessions/<session-id>/summary.md` | 写入完整会话总结（同对话输出的内容但更详细） |
| P1（如有） | `docs/decisions.md` | 追加本次会话产生的新决策 |
| P1（如有） | `docs/LEARNINGS.md` | 追加从本次 Bug/失败中提取的教训 |
| P1（如有） | `docs/success_cases.json` | 追加本次成功交付的可复用模式 |
| P2（按需） | `docs/AGENT_PROMPT_TEMPLATES.md` | 如果发现新的有效 Prompt 模式或需要修正 |
| P2（按需） | `docs/ARCHITECTURE.md` | 如果架构发生了变化 |
| P2（按需） | `docs/CODING_STANDARDS.md` | 如果人类提出了新的编码规范要求 |

#### 3.3.3 Session ID 命名规范

```
格式：YYYYMMDD-NN
示例：20260531-01（2026年5月31日的第1次会话）
    20260531-02（同日第2次会话）
```

---

## 4. 质量保障机制

### 4.1 AI 提交代码前的自检项

以下检查项按照严重性分为三个等级：

#### 🔴 阻断级（必须通过，否则禁止提交）

| # | 检查项 | 检查方式 |
|---|--------|----------|
| 1 | 代码可通过编译/解释器加载 | `python -m py_compile` 或等价命令 |
| 2 | 类型检查通过 | `mypy --strict`（Python）或等价 |
| 3 | 无硬编码密钥/Token/密码 | `rg` 扫描常见密钥模式 |
| 4 | 无 `print()` / `console.log()` 遗留 | `rg` 扫描调试输出 |
| 5 | 无未解决的 merge conflict 标记 | `rg "<<<<<<<|>>>>>>>|======="` |
| 6 | 无裸 `except:` 或 `except Exception:` 吞异常 | 代码审查 |

#### 🟡 重要级（应当通过，有例外需注释说明）

| # | 检查项 | 检查方式 |
|---|--------|----------|
| 7 | 所有公共函数/类有 Docstring | 人工 + `pylint` / `eslint` |
| 8 | 所有外部输入有校验 | 人工审查 |
| 9 | 数据库查询使用参数化（防注入） | 人工审查 |
| 10 | 文件路径操作使用安全 API（防路径遍历） | 人工审查 |
| 11 | 新增依赖已写入 requirements / package.json | `diff` 检查 |

#### 🟢 建议级（鼓励通过）

| # | 检查项 | 检查方式 |
|---|--------|----------|
| 12 | 格式化符合项目配置 | `black --check` / `prettier --check` |
| 13 | 无过长的函数（>50 行）或文件（>500 行） | `cloc` / `tokei` |
| 14 | 测试覆盖率不降低 | 比较前后的 coverage 报告 |
| 15 | 变更文件数不超过 10 个（PR 粒度） | `git diff --stat` |

### 4.2 人类最终审查点

AI 应明确告知人类：以下审查点需要人类的判断力，AI 无法替代：

| 审查维度 | 审查问题 |
|----------|----------|
| **架构合理性** | 这个变更引入的抽象层次是否合理？是否过度工程化？ |
| **安全性** | 权限设计、数据暴露面、认证流程是否安全？ |
| **业务逻辑正确性** | 是否真的解决了用户的问题？（AI 可能误解需求） |
| **可维护性** | 6 个月后一个新接手的人能看懂吗？ |
| **与现有系统的一致性** | 新代码的风格、命名、模式是否与现有代码一致？ |

AI 在提交 PR 时，应在 PR 描述中主动标注：
```
🤖 AI 自评：
- 自检清单：[全部通过 / 通过 X 项，N 项不适用]
- 不确定项：[列出不确定需要人类重点审查的点]
- 建议审查重点：[1-3 个建议人类重点关注的方面]
```

### 4.3 质量度量方法

| 指标 | 计算方法 | 记录位置 | 目标值 |
|------|----------|----------|--------|
| **审查重试次数** | 同一 PR 被打回的次数 | 每次在 session 总结中记录 | 平均 < 1.5 |
| **缺陷密度** | 上线后 7 天内发现的 Bug 数 / 变更的 KLOC | LEARNINGS.md 汇总 | < 2 bugs/KLOC |
| **记忆库有效利用率** | 被引用 ≥2 次的记忆条目数 / 总记忆条目数 | 月度回顾时统计 | > 60% |
| **自检拦截率** | 被自检查出的问题数 / 总问题数 | 每次 PR 记录 | > 80% |
| **会话衔接成功率** | 新 AI 实例仅凭记忆文件就能独立开始工作的比率 | 每次新会话评估 | > 90% |

---

## 5. 经验沉淀与进化

### 5.1 从成功中提取通用模式

**触发条件**：一个功能模块通过人类审查且稳定运行 ≥3 天。

**执行步骤**：
1. 识别该模块中可复用的**代码模式**（不一定是整段代码，可以是设计套路）。
2. 提取模式的**触发条件**（什么需求场景下应该想到这个模式）。
3. 提取模式的**关键代码片段路径+行号**（不复制整段代码，只引用位置）。
4. 写入 `docs/success_cases.json`，按照 §2.2.4 的格式。
5. 如果这个模式足够通用，同时更新 `AGENT_PROMPT_TEMPLATES.md`。

**示例**：
```json
{
  "id": "case-007",
  "title": "带重试机制的外部 API 调用包装器",
  "pattern": "装饰器 + 指数退避 + 断路器",
  "prompt_keywords": ["API调用", "重试", "容错", "外部服务"],
  "reusable_pattern_snippet": "src/utils/api_retry.py#L20-L55"
}
```

### 5.2 从失败中提取教训

**触发条件**：任何导致以下后果的事件发生时：
- 人类审查打回代码
- 测试失败暴露设计缺陷
- 线上 Bug
- AI 误解需求导致返工

**执行步骤**：
1. **立即记录**：在 `docs/LEARNINGS.md` 中按 §2.2.3 格式创建教训条目。
2. **根因溯源**：追问 3 层 "为什么" 来找到真正的根因（不仅是表象）。
3. **Prompt 改进判定**：
   - 如果根因是 "AI 不知道应该检查某个方面" → 更新 `AGENT_PROMPT_TEMPLATES.md` 中的自检清单。
   - 如果根因是 "AI 知道该检查但忘了" → 在对应的 Prompt 模板中增加强调标记。
   - 如果根因是 "人类需求表述模糊" → 更新 §3.2.3 的澄清触发条件。
4. **关联**：将教训条目与决策条目（如有相关决策）交叉引用。

**教训根因分析方法示例**：
```
现象：JWT Token 过期处理导致 500 错误
├── 为什么？→ 代码没有 try-catch ExpiredSignatureError
│   └── 为什么？→ AI 生成代码时只处理了 DecodeError，没考虑过期
│       └── 为什么？→ 自检清单中没有 "所有 JWT 操作需处理 3 种异常"
│           └── 根因 → 自检清单不完整
├── 修复：代码层面加异常处理
└── 预防：在自检清单中加入 "JWT 操作：DecodeError + ExpiredSignatureError + InvalidTokenError"
```

### 5.3 中长期仪式

#### 5.3.1 每周回顾（每 7 天或每 5 次会话后）

AI 应在会话开始时（如果距上次回顾 ≥7 天）主动提议执行回顾：

**回顾清单**：
```markdown
## 每周回顾 — 第 X 周（YYYY-MM-DD ~ YYYY-MM-DD）

### 统计
- 完成功能数：X
- 新增代码行数：+X / -Y
- 发现 Bug 数：X（其中 AI 引入的：Y）
- 平均审查轮次：X 轮
- 新增决策数：X
- 新增教训数：X

### 模式发现
- 重复出现的问题类型：[如有]
- 可复用的成功模式：[如有]

### 记忆库健康
- total 条目数：X
- 有效利用率：X%
- 需要清理的过时条目：[列出]

### 下阶段优化建议
1. [基于本周的数据提出的改进建议]
```

#### 5.3.2 版本发布前检查

```markdown
## 发布前 AI 辅助清单

- [ ] 运行完整测试套件，确认全部通过
- [ ] 检查 LEARNINGS.md 中是否有标记为 "未解决" 的高优教训
- [ ] 检查 CURRENT_STATUS.md 中的 "已知问题" 是否全部分配到版本或降级
- [ ] 生成 CHANGELOG.md 草稿
- [ ] 检查依赖是否有已知安全漏洞（`pip-audit` / `npm audit`）
- [ ] 检查是否有需要废弃的决策（decisions.md 中标记为 "已废弃"）
- [ ] 更新 CURRENT_STATUS.md 中已部署环境信息
```

---

## 6. 沟通与输出规范

### 6.1 AI 输出的格式要求

#### 6.1.1 引用文件时

**必须使用完整相对路径 + 行号**：
```
✅ src/auth/login_handler.py#L45-L78
❌ "在 login_handler.py 里面那一段"
```

#### 6.1.2 代码块

**必须标注语言**：
````
```python
# 代码
```
````

#### 6.1.3 进度报告

使用统一模板：
```
📊 进度 | 已完成 3/7 | 进行中：用户认证模块
```

#### 6.1.4 建议/警告/错误

使用统一的视觉前缀：
```
💡 建议：这是一个优化方向……
⚠️ 警告：这可能会影响……
🚫 错误：这会导致……
ℹ️ 注意：补充信息……
```

### 6.2 人类给出任务时应包含的信息

> **推荐模板**（人类可以使用此模板来确保任务描述完整）：

```markdown
## 任务：[简短标题]

### 背景
[为什么需要这个任务？与什么功能/问题相关？]

### 目标
[具体要达成什么？可量化的验收标准是什么？]

### 约束
- 技术约束：[必须使用的库、框架、模式]
- 时间约束：[是否紧急]
- 范围约束：[明确不做什么]

### 期望输出
- [ ] 产出 1：具体文件/功能描述
- [ ] 产出 2：测试要求
- [ ] 产出 3：文档要求

### 相关上下文
- 相关文件：[路径]
- 相关决策：[决策编号]
- 参考材料：[链接或路径]
```

### 6.3 AI 主动请求澄清的触发条件

将 §3.2.3 中的触发条件细化为可操作的判断逻辑：

```python
# 伪代码：AI 是否应该主动请求澄清？
def should_ask_for_clarification(task: Task) -> bool:
    # 1. 多选一方案无压倒性优势
    if ambiguous_options(task) and not clear_winner(task):
        return True
    # 2. 输入/输出的格式未在上下文或记忆文件中定义
    if undefined_io_format(task) and not in_memory_files(task):
        return True
    # 3. 边界条件会影响实现方式
    if missing_boundary_conditions(task):
        return True
    # 4. 任务与已记录的决策冲突
    if conflicts_with_decision(task, decisions_db):
        return True
    # 5. 涉及安全但不明确安全标准
    if has_security_implications(task) and not defined_security_policy(task):
        return True
    return False
```

---

## 7. 工具与自动化

### 7.1 建议的辅助脚本

#### 7.1.1 会话启动脚本（`scripts/session_start.sh`）

```bash
#!/bin/bash
# 会话启动脚本 - 人类一键初始化 AI 协作环境

SESSION_ID=$(date +%Y%m%d)-$(printf "%02d" $(( $(ls sessions/ 2>/dev/null | grep $(date +%Y%m%d) | wc -l) + 1 )))
mkdir -p "sessions/${SESSION_ID}"

echo "🧠 DevFlow CI 会话启动"
echo "  会话 ID: ${SESSION_ID}"
echo "  日期: $(date '+%Y-%m-%d %H:%M:%S')"
echo ""
echo "📂 记忆文件状态:"
for f in AI_COLLABORATION_GUIDE.md CURRENT_STATUS.md docs/ARCHITECTURE.md docs/CODING_STANDARDS.md docs/LEARNINGS.md docs/AGENT_PROMPT_TEMPLATES.md docs/decisions.md docs/success_cases.json; do
    if [ -f "$f" ]; then
        echo "  ✅ $f ($(wc -l < "$f") 行)"
    else
        echo "  ❌ $f (缺失)"
    fi
done
echo ""
echo "💡 接下来：启动 AI 会话，AI 将自动读取以上所有文件。"
```

#### 7.1.2 记忆检索 CLI（`scripts/memory_search.py`）

```python
"""
记忆检索工具 - 允许人类和 AI 快速检索记忆文件中的内容。
用于查历史决策、教训、成功案例等。
"""
# 建议实现：基于 ripgrep + 结构化查询的简单检索工具
# 支持按类型、日期范围、关键词过滤
```

#### 7.1.3 会话质量评分（`scripts/session_quality.py`）

```python
"""
会话质量评分 - 在会话结束后由 AI 运行，评估记忆外化的完整性。
检查项：
  - session summary 是否已写
  - CURRENT_STATUS.md 是否已更新
  - 如果有新决策/教训，是否已写入对应文件
  - 变更文件清单是否完整
输出：0-100 的会话质量分
"""
```

### 7.2 Git 钩子集成

#### 7.2.1 Pre-commit Hook（`.git/hooks/pre-commit`）

```bash
#!/bin/bash
# AI 代码自检 - pre-commit

echo "🔍 运行 AI 代码自检..."

# 🔴 阻断级检查
echo "  [1/6] 编译检查..."
# python -m py_compile ...（根据项目语言调整）

echo "  [2/6] 密钥泄露扫描..."
# rg '(api.?key|secret|password|token)\s*=\s*["\'][^"\' ]{20,}' --glob '!AI_COLLABORATION_GUIDE.md'

echo "  [3/6] 调试代码检查..."
# rg '(print\(|console\.log|TODO.*FIXME)' 

echo "  [4/6] Merge 冲突检查..."
# rg '<<<<<<<|>>>>>>>|======='

echo "  [5/6] 类型检查..."
# mypy --strict ...（根据项目配置）

echo "  [6/6] 格式化检查..."
# black --check ...（根据项目配置）

if [ $? -ne 0 ]; then
    echo ""
    echo "🚫 阻断级检查未通过，请修复后重新提交。"
    echo "💡 参考：AI_COLLABORATION_GUIDE.md 第 4 章"
    exit 1
fi

echo "✅ 所有阻断级检查通过。"
echo ""
echo "⚠️ 请人工确认以下检查项："
echo "  - 架构合理性"
echo "  - 业务逻辑正确性"
echo "  - 安全敏感操作"
```

### 7.3 演进路线图

| 阶段 | 工具/能力 | 描述 | 依赖 |
|------|----------|------|------|
| **Phase 1**（立即） | 记忆文件骨架 | 创建所有 §2.1 中列出的文件 | 本指南 |
| **Phase 1**（立即） | 会话启动/结束仪式 | AI 按 §3.1 和 §3.3 执行 | 记忆文件存在 |
| **Phase 2**（✅ 2026-06-01） | session_start/end.sh/.bat | 会话启动与质量检查自动化 | phase 1 稳定 |
| **Phase 2**（✅ 2026-06-01） | test_flow.bat + 心跳测试 | E2E 冒烟脚本不再假卡住 | phase 1 稳定 |
| **Phase 2**（进行中） | pre-commit 钩子 | 阻断级自检自动化 | scripts/pre_commit_check.sh |
| **Phase 3**（✅ 2026-06-01） | memory_search.py | 记忆检索 CLI | 记忆条目 > 20 |
| **Phase 3**（✅ 2026-06-01） | session_end.sh 质量检查 | 会话结束质量评分（0–100） | 3+ 次会话完成 |
| **Phase 4**（2 个月+） | 经验自动聚类 | 从 LEARNINGS.md 中自动发现高频模式 | 教训条目 > 50 |
| **Phase 4**（2 个月+） | Prompt 自动调优 | 基于审查重试数据自动优化 Prompt 模板 | 审查数据 > 20 |
| **Long-term** | CI 集成记忆验证 | CI 流程中自动检查记忆文件是否过期 | 项目 CI 就绪 |

---

## 8. 附录

### 8.1 文件模板快速生成

当记忆文件缺失时，AI 可以使用以下命令快速初始化：

```bash
# 初始化所有记忆文件骨架
mkdir -p docs sessions
touch CURRENT_STATUS.md
touch docs/ARCHITECTURE.md docs/CODING_STANDARDS.md docs/LEARNINGS.md
touch docs/AGENT_PROMPT_TEMPLATES.md docs/decisions.md
echo '{"version":"1.0.0","cases":[]}' > docs/success_cases.json
```

### 8.2 术语表

| 术语 | 定义 |
|------|------|
| **记忆文件** | 存储在项目文件系统中的、跨会话持久化的上下文文件（见 §2.1） |
| **会话** | 从人类启动 AI 对话到本次对话结束的完整周期 |
| **Session ID** | 格式为 YYYYMMDD-NN 的会话唯一标识符 |
| **ADR** | Architecture Decision Record，架构决策记录 |
| **外化** | 将信息从对话短期上下文转移到文件长期存储的过程 |
| **自检清单** | AI 在提交代码前必须逐项核对的检查项列表（见 §4.1） |
| **审查轮次** | 同一 PR 被人类打回修改的次数 |

### 8.3 本文件版本历史

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|----------|------|
| v1.0.0 | 2026-05-31 | 初始版本，建立全部 7 章协作规范 | AI + 人类 |

---

> **本文件是 DevFlow CI 项目的"操作系统"。它定义了人与 AI 之间、AI 实例与 AI 实例之间如何协作。请像维护代码一样维护它。**
>
> 当协作模式发现不足时，**不要绕过它——改进它**。提交 PR 来更新本文件。
